# Environment & secrets
.env
.env.local
.env.*.local
.env.ubuntu
*.pem
*.key
credentials.json

# IDE
.vscode/
.idea/
*.iml

# Claude Code
.claude/
CLAUDE.md

# Local docs / analysis notes (not committed)
docs/key-insights.md

# macOS
.DS_Store
.DS_Store?
._*

# ── Build / dependency / runtime artefacts (never commit) ──
node_modules/
dist/
dist-ssr/
build/
out/
.gradle/
.vite/
coverage/
test-results/

# Local runtime data (blobs, derivation cache under HELIX_*_DISK)
var/
**/var/

# Service-local blob store (content-addressed; regenerated from uploads)
tb-helix-ai-svc/var/

# UI sample PDFs / TIFFs (local only; not needed in repo)
tb-helix-ai-ui/public/samples/

# Large binaries / installers under test/
test/Ollama.dmg
test/*.dmg

# Python venvs (local dev)
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
*.egg

# Docker
docker-compose.override.yml

# PostgreSQL data directory (runtime data, never commit)
infra/postgres/data/

# Logs
*.log
logs/

# Temp files
*.tmp
*.swp
*.swo
~
